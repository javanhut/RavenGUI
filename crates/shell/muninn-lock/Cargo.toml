[package]
name = "muninn-lock"
description = "The RavenLinux lock screen. A separate binary from muninn on purpose: ext-session-lock-v1 keeps the session locked if this process dies, which only holds if a shell crash cannot take it down."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
tracing.workspace = true
tracing-subscriber.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
smithay-client-toolkit.workspace = true

[lints]
workspace = true
