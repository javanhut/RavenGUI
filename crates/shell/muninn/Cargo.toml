[package]
name = "muninn"
description = "The RavenLinux desktop shell: panel, launcher, notifications. A Wayland client on wlr-layer-shell, deliberately a separate process from the compositor."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
raven-config.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
raven-protocol = { workspace = true, features = ["client"] }
smithay-client-toolkit.workspace = true
iced.workspace = true

[lints]
workspace = true
