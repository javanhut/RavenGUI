[package]
name = "raven-config"
description = "Shared configuration schema for the Huginn compositor and the Muninn shell. Host-agnostic: builds and tests anywhere."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true

[lints]
workspace = true

[dev-dependencies]
toml = "0.9"
