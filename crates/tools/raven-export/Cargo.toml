[package]
name = "raven-export"
description = "Turn a Raven screen recording into an MP4 that anything plays."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[[bin]]
name = "raven-export"
path = "src/main.rs"

# Only the tree's own crates: the recording format, the encoder and the MP4
# writer. The colour conversion is written here.
[dependencies]
raven-rec.workspace = true
raven-h264.workspace = true
raven-mp4.workspace = true

[lints]
workspace = true
